# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
.idea/

# macOS
.DS_Store

# Claude Code per-user settings (user-specific, not repo-wide policy)
.claude/settings.local.json

# Lektorat audit run-snapshots: transient, point-in-time tool output. Provenance
# for applied fixes lives in the commit message, not in tracked snapshots.
# (Persistent review-plan artefacts elsewhere under .audits/ may still be tracked.)
.audits/lektorat/
