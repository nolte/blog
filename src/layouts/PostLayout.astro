---
import BaseLayout from "~/layouts/BaseLayout.astro";
import type { CollectionEntry } from "astro:content";
import { useTranslations } from "~/i18n/ui";

interface Props {
  post: CollectionEntry<"posts">;
  translationHref?: string;
}

const { post, translationHref } = Astro.props;
const lang = post.data.lang;
const t = useTranslations(lang);
const dateLocale = lang === "de" ? "de-DE" : "en-US";
const pubFormatted = post.data.pubDate.toLocaleDateString(dateLocale, {
  year: "numeric",
  month: "long",
  day: "numeric",
});
const updatedFormatted = post.data.updatedDate?.toLocaleDateString(dateLocale, {
  year: "numeric",
  month: "long",
  day: "numeric",
});
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const blogHref = lang === "en" ? `${base}/blog` : `${base}/de/blog`;
---

<BaseLayout
  title={post.data.title}
  description={post.data.description}
  lang={lang}
  ogImage={post.data.heroImage}
>
  <article class="prose max-w-none">
    <header class="mb-8 not-prose">
      <h1 class="text-3xl font-bold tracking-tight">{post.data.title}</h1>
      <p class="mt-2 text-muted">{post.data.description}</p>
      <div class="mt-3 flex flex-wrap items-center gap-x-4 gap-y-2 text-xs text-faint">
        <time datetime={post.data.pubDate.toISOString()}>
          {t("post.published")}: {pubFormatted}
        </time>
        {updatedFormatted && (
          <span>
            {t("post.updated")}: {updatedFormatted}
          </span>
        )}
        {post.data.aiGenerated && <span class="rounded bg-accent px-1.5 py-0.5 font-medium text-accent-fg">AI</span>}
        {translationHref && (
          <a href={translationHref} class="text-link underline hover:no-underline">
            {t("post.translation")}
          </a>
        )}
      </div>
      {post.data.tags.length > 0 && (
        <ul class="mt-3 flex flex-wrap gap-2 text-xs">
          {post.data.tags.map((tag) => (
            <li class="rounded bg-surface-subtle px-2 py-0.5 font-mono text-muted">#{tag}</li>
          ))}
        </ul>
      )}
    </header>
    <slot />
    <hr class="my-10" />
    <p class="text-sm">
      <a href={blogHref}>{t("post.backToBlog")}</a>
    </p>
  </article>
</BaseLayout>
